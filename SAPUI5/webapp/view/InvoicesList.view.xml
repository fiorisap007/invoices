<mvc:View   controllerName="nsdprb.SAPUI5.controller.InvoicesList"
            xmlns="sap.m" xmlns:mvc="sap.ui.core.mvc">
    <List id="invoiceList" headerText="{i18n>invoiceListTitle}" class="sapUiResponsiveMargin" width="auto" items="{invoices>/Invoices}">
        <headerToolbar>
            <Toolbar id="tb">
                <Title id="tt" text="{i18n>invoiceListTitle}"></Title>
                <ToolbarSpacer id="tsp"></ToolbarSpacer>
                <SearchField id="sf" width="50%" search=".onFilterInvoices" ></SearchField>
            </Toolbar>
        </headerToolbar>
        <items>
            <ObjectListItem id="oli01" 
                            title="{invoices>Quantity} x {invoices>ProductName}" 
                            number="{ parts : [ { path: 'invoices>ExtendedPrice'},
                                                { path: 'currency>/eur'}],
                                      type: 'sap.ui.model.type.Currency',
                                      formatOptions:{
                                            showMeasure:false
                                      }
                                    }"
                            numberUnit="{currency>/eur}"
                            numberState="{= ${invoices>ExtendedPrice} > 9 ? 'Success': 'Error'}"   
                                    >
                <firstStatus>
                    <ObjectStatus id="os1" text="{ path: 'invoices>Status', formatter: '.formatter.invoiceStatus'}"></ObjectStatus>
                </firstStatus>
            </ObjectListItem>            
        </items>
    </List>
</mvc:View>
